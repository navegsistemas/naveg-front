{
  "name": "naveg-front",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Agência virtual da NAVEG — Navegação e Logística. Single page institucional com o totem de reserva.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --build --force",
    "verify": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^5.9.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=22"
  }
}
